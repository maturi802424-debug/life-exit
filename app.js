import * as THREE from 'three';
const $=s=>document.querySelector(s);const scene=new THREE.Scene();scene.background=new THREE.Color(0x030606);scene.fog=new THREE.FogExp2(0x050909,.032);const camera=new THREE.PerspectiveCamera(68,innerWidth/innerHeight,.1,120);camera.position.set(0,1.65,7);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;$('#game').appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0x82918b,0x080909,.35));const corridor=new THREE.Group();scene.add(corridor);const mat=(c,r=.7,m=.05)=>new THREE.MeshStandardMaterial({color:c,roughness:r,metalness:m}),wall=mat(0xb6b1a1,.92),floor=mat(0x313535,.75,.15),dark=mat(0x111515,.8);function box(x,y,z,w,h,d,material){const o=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),material);o.position.set(x,y,z);o.receiveShadow=o.castShadow=true;corridor.add(o);return o}box(0,-.08,-25,6,.16,70,floor);box(-3.05,1.8,-25,.12,3.8,70,wall);box(3.05,1.8,-25,.12,3.8,70,wall);box(0,3.72,-25,6.2,.12,70,dark);for(let z=5;z>-58;z-=7){box(0,3.55,z,3.8,.05,.45,new THREE.MeshStandardMaterial({color:0xe9e2c8,emissive:0xb7a86e,emissiveIntensity:1.3}));const l=new THREE.PointLight(0xfff0bf,1.5,12,2);l.position.set(0,3.35,z);corridor.add(l)}for(let z=1;z>-54;z-=8){box(-2.97,1.45,z,.08,1.45,2.2,dark);box(2.97,1.45,z,.08,1.45,2.2,dark)}box(0,1.6,-58,4.2,3.2,.25,dark);const exitSign=box(0,2.75,-57.82,1.5,.45,.05,new THREE.MeshStandardMaterial({color:0x183b28,emissive:0x0e7c3b,emissiveIntensity:1.2}));
const themes=[['健康','休むことは、前進を止めることだ。'],['人間関係','大切な人との時間は、いつでも取り戻せる。'],['自己成長','失敗しないことが、成長の証だ。'],['仕事','忙しい。それだけで前進している。'],['経済','未来のお金は、未来の自分が何とかする。'],['空間・環境','環境が変わるまで、自分は変われない。'],['自由な時間','やりたいことは、時間ができてから。'],['貢献','自分を満たす前に、誰かを満たさなければならない。']],normals=['焦らなくていい。現在地を見よう。','今日の選択が、10年後の景色をつくる。','大切なものほど、静かにそこにある。','進むことと、立ち止まること。どちらも選択だ。'];let level=0,anomaly=false,active=false,deciding=false,z=7,yaw=0,pitch=0,roundLocked=false,audio,joyX=0,joyY=0;
function sound(freq=180,d=.12,type='sine',vol=.06){if(!audio)return;const o=audio.createOscillator(),g=audio.createGain();o.type=type;o.frequency.value=freq;g.gain.setValueAtTime(vol,audio.currentTime);g.gain.exponentialRampToValueAtTime(.001,audio.currentTime+d);o.connect(g).connect(audio.destination);o.start();o.stop(audio.currentTime+d)}function resetVisual(){exitSign.rotation.z=0;scene.fog.density=.032;corridor.rotation.z=0}function newRound(){roundLocked=false;deciding=false;document.body.classList.remove('deciding');$('#decision').classList.add('hidden');$('#guide').textContent='前へ進み、通路を観察してください';anomaly=Math.random()<.58;const t=themes[level%8];$('#theme').textContent=anomaly?t[0]:'現在地';$('#message').textContent=anomaly?t[1]:normals[Math.floor(Math.random()*normals.length)];z=7;camera.position.set(0,1.65,z);yaw=0;pitch=0;camera.rotation.set(0,0,0);resetVisual();if(anomaly){const kind=Math.floor(Math.random()*4);if(kind===0)exitSign.rotation.z=.15;else if(kind===1)scene.fog.density=.055;else if(kind===2){setTimeout(()=>{if(active&&!deciding){$('#flash').style.opacity=.45;setTimeout(()=>$('#flash').style.opacity=0,110)}},1200)}else corridor.rotation.z=.008}}
function showDecision(){if(deciding||roundLocked)return;deciding=true;document.body.classList.add('deciding');$('#decision').classList.remove('hidden');$('#guide').textContent='異変があったか判断してください';sound(280,.1,'sine',.035)}function decide(turnBack){if(roundLocked||!deciding)return;roundLocked=true;const correct=turnBack===anomaly;sound(correct?520:75,.35,correct?'sine':'sawtooth',.09);$('#flash').style.background=correct?'#d8ffd8':'#8b0000';$('#flash').style.opacity=.32;setTimeout(()=>$('#flash').style.opacity=0,180);if(correct){level++;if(level>=8){setTimeout(endGame,500);return}}else level=0;$('#level b').textContent=level;$('#decision').classList.add('hidden');document.body.classList.remove('deciding');setTimeout(newRound,650)}function endGame(){active=false;deciding=false;document.exitPointerLock?.();$('#ending').classList.remove('hidden');$('#reflection').innerHTML='あなたが今、最も小さな「異変」に気づきたいのはどこですか？<br><b>健康・人間関係・自己成長・仕事・経済・環境・時間・貢献</b>';sound(660,.8,'sine',.05)}
$('#back').onclick=()=>decide(true);$('#forward').onclick=()=>decide(false);$('#startBtn').onclick=()=>{audio=new (window.AudioContext||window.webkitAudioContext)();$('#start').classList.add('hidden');active=true;$('#joystick').classList.remove('hidden');newRound();sound(220,.4,'sine',.04)};$('#again').onclick=()=>{level=0;$('#level b').textContent=0;$('#ending').classList.add('hidden');active=true;newRound()};
const keys={};addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;if(deciding&&e.key.toLowerCase()==='q')decide(true);if(deciding&&e.key.toLowerCase()==='e')decide(false)});addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);renderer.domElement.addEventListener('click',()=>{if(active&&!deciding&&matchMedia('(pointer:fine)').matches)renderer.domElement.requestPointerLock?.()});addEventListener('mousemove',e=>{if(document.pointerLockElement===renderer.domElement&&!deciding){yaw-=e.movementX*.002;pitch=Math.max(-.55,Math.min(.55,pitch-e.movementY*.0015));camera.rotation.order='YXZ';camera.rotation.y=yaw;camera.rotation.x=pitch}});
const joy=$('#joystick'),stick=$('#stick');function joyMove(e){e.preventDefault();const t=e.touches[0],r=joy.getBoundingClientRect(),dx=t.clientX-(r.left+r.width/2),dy=t.clientY-(r.top+r.height/2),m=Math.min(38,Math.hypot(dx,dy)),a=Math.atan2(dy,dx);joyX=Math.cos(a)*m/38;joyY=Math.sin(a)*m/38;stick.style.transform=`translate(${joyX*34}px,${joyY*34}px)`}function joyEnd(){joyX=joyY=0;stick.style.transform='translate(0,0)'}joy.addEventListener('touchstart',joyMove,{passive:false});joy.addEventListener('touchmove',joyMove,{passive:false});joy.addEventListener('touchend',joyEnd);let lx,ly;$('#lookZone').addEventListener('touchstart',e=>{lx=e.touches[0].clientX;ly=e.touches[0].clientY},{passive:false});$('#lookZone').addEventListener('touchmove',e=>{e.preventDefault();const t=e.touches[0],dx=t.clientX-lx,dy=t.clientY-ly;lx=t.clientX;ly=t.clientY;yaw-=dx*.005;pitch=Math.max(-.5,Math.min(.5,pitch-dy*.004));camera.rotation.order='YXZ';camera.rotation.y=yaw;camera.rotation.x=pitch},{passive:false});
let last=performance.now(),step=0;function loop(now){requestAnimationFrame(loop);const dt=Math.min((now-last)/1000,.05);last=now;if(active&&!deciding){let f=(keys.w||keys.arrowup?1:0)-(keys.s||keys.arrowdown?1:0)-joyY,s=(keys.d?1:0)-(keys.a?1:0)+joyX,moving=Math.abs(f)+Math.abs(s)>.08;if(moving){const speed=3.15;z-=Math.cos(yaw)*f*speed*dt;camera.position.x+=Math.sin(yaw)*f*speed*dt+Math.cos(yaw)*s*speed*dt;camera.position.x=Math.max(-2.35,Math.min(2.35,camera.position.x));z=Math.max(-49,Math.min(7,z));camera.position.z=z;camera.position.y=1.65+Math.sin(now*.009)*.025;if(now-step>480){sound(90,.055,'triangle',.022);step=now}}if(z<-38)showDecision()}renderer.render(scene,camera)}requestAnimationFrame(loop);addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});